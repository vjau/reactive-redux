{
  "name": "tracker-redux-source",
  "version": "1.0.0",
  "description": "",
  "files": ["lib"],
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "npm run clean && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel index.js -d lib",
    "clean": "rimraf lib"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-preset-es2015": "^6.22.0",
    "rimraf": "^2.6.1"
  },
  "dependencies": {
    "object-path": "^0.11.4"
  }
}
